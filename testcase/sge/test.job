#!/bin/bash
#$ -cwd
#$ -o ./result/$JOB_ID.$TASK_ID.out  # 标准输出文件的位置
#$ -j y                # 将标准错误合并到标准输出
#$ -N SGE_Test         # 作业名称
#$ -pe smp 1
#$ -l h_vmem=10M

echo "start:$(date +%s.%N):$$"
sleep 100
echo "end:$(date +%s.%N)"

